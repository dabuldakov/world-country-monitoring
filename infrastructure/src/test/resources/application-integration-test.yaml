application:
  scheduling:
    update-all-countries-gdp: "0 0 0 1 1 ?"
  frontend:
    client-key: wcm-test-client
  refill:
    worker:
      enabled: false
  admin:
    login: admin
    password: test-admin
    token-secret: test-secret
    email: admin@example.com

spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
